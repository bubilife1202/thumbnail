# 🧭 단일 태스크: 레이아웃/템플릿 정렬 정비

## 목표
- 화면 전역 레이아웃을 공통 스캐폴딩으로 통일하고, 템플릿 레이아웃과 기본 폭(100%) 규칙을 일관되게 적용한다.
- 간격/타이포 토큰을 표준화해 "레이아웃 불일치"를 제거하고 QA 기준선을 마련한다.

## 범위
- 공통 레이아웃 컴포넌트, 템플릿 스캐폴딩, "쓰고 싶은" 템플릿 패키지, 폭·간격 토큰, 회귀 QA 캡처/체크리스트.

## 완료 조건 (Definition of Done)
1) **공통 레이아웃 컴포넌트 적용**
   - `components/layout/`에 `PageContainer`, `ContentGrid`, `Sidebar`, `TemplateShell` 등 공통 컨테이너를 정의하고 모든 주요 페이지가 이를 사용하도록 마이그레이션.
   - Breakpoint·gap·padding을 토큰으로 정의해 props로 제어 가능.

2) **템플릿 스캐폴딩 정합성 확보**
   - 헤더/푸터/사이드바/메인 영역의 기본 높이·폭·배경·z-index를 토큰으로 선언하고, 모바일에서는 사이드바가 드로어/오버레이로 전환되도록 반응형 규칙 적용.
   - 모든 페이지를 `<TemplateShell><PageContent/></TemplateShell>` 형태로 래핑해 템플릿 구조가 일치함을 확인.

3) **템플릿 품질 및 선호도 향상(“쓰고 싶은 템플릿”)**
   - 역할/도메인별로 즉시 사용 가능한 프리셋(예: 대시보드, 랜딩, 폼 위주, 콘텐츠/블로그, 헬프센터)을 4~6종 준비하고, 시각적 요소(컬러 팔레트/타이포/카드 스타일)를 토큰 기반으로 다크/라이트 지원.
   - 템플릿 갤러리(프리뷰/스크린샷/기본 컴포넌트 목록)를 문서화하거나 샌드박스에 배포해 선택을 용이하게 하고, “기본값”을 가장 보편적인 템플릿으로 지정.
   - 템플릿 프리셋은 모두 공통 스캐폴딩과 동일한 토큰을 사용하며, 커스터마이즈 포인트(헤더 높이, 사이드바 모드, 카드 스타일 등)를 명확히 노출.

4) **기본 폭 100% + max-width 중앙 정렬**
   - 루트/메인 컨테이너에 `width: 100%`를 유지하되 `max-width` 토큰(예: 1200~1440px)을 적용하고 `margin: 0 auto`로 중앙 정렬.
   - 내부 카드·폼 등은 최소/최대 폭을 정의해 과도한 수축·확대를 방지.

5) **Spacing & Typography 스케일 토큰화**
   - spacing scale(예: 4/8/12/16/24/32px)과 타이포 스케일을 테마/토큰으로 정의하고 CSS 변수로 노출.
   - 임의 margin/padding을 모두 토큰 값으로 치환하고, 텍스트 스타일은 공통 `Text` 또는 유틸 클래스로 통일.

6) **QA 기준선 및 회귀 스냅샷**
   - 대표 화면(홈, 리스트, 상세, 폼)을 브레이크포인트별로 캡처해 기준선을 마련하고 QA 체크리스트(정렬/폭/간격/템플릿 구조)를 문서화.
   - stylelint 등으로 임의 spacing 사용을 방지하는 규칙을 추가/검토.

## 산출물
- 토큰/컴포넌트/레이아웃/템플릿 패키징 적용 PR 1건.
- QA 캡처 및 체크리스트 문서 1건.

## 작업 순서(예시)
1. 디자인 토큰 정의: spacing·타이포·max-width 변수 선언.
2. 공통 레이아웃 컴포넌트 작성 후 페이지 마이그레이션.
3. 템플릿 스캐폴딩(헤더/푸터/사이드바/메인) 반응형 규칙 구현.
4. 도메인별 템플릿 프리셋/갤러리(샌드박스 또는 문서) 구성 및 기본값 지정.
5. 폭(100% + max-width)과 간격 토큰 적용, 임의 스타일 정리.
6. QA 캡처 및 체크리스트 작성, stylelint 규칙 검토.
